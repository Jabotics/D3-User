services:
  d3-user:

    build:
      context: .
      dockerfile: Dockerfile.dev

    ports:
      - "3000:5173"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      - CHOKIDAR_USEPOLLING=true
      
    command: npm run dev
